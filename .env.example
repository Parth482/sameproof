# Use a MongoDB Atlas SRV connection string. Never expose this with NEXT_PUBLIC_.
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@CLUSTER_HOST/sameproof?retryWrites=true&w=majority
MONGODB_DB_NAME=sameproof

# Development-only fallback. Set false in deployed environments.
SAMEPROOF_DEMO_MODE=true
